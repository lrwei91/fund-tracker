{
  "name": "fund-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "A-share investment dashboard with serverless API proxies.",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "check": "node --check app.js && for f in api/*.js scripts/*.js; do node --check \"$f\"; done && node scripts/dom-contract-test.js",
    "test:api": "node scripts/api-smoke-test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
